authentik:
  config:
  secret_key: ${authentik_token}
  postgresql:
    host: "postgresql.database.svc.cluster.local"
    name: "authentik"
    user: "authentik_user"
    password: ${password}
    port: 5432
server:
  ingress:
    enabled: true
    hosts:
      - auth.svc.jpruitt.dev
postgresql:
  enabled: false


