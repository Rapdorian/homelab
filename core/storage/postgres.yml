passwordupdatejob:
  enabled: true
auth:
  postgresPassword: "${password}"
metrics:
  enabled: true
  service: 
    port: 9187
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"